进行修改之前，请先询问我的回答后再继续。请最简实现，不要过度设计。

需求具体的内容均以doc路径下的文档为准。

前后端应用都接入了热加载机制，如果没有较大的改动，不需要终止现有的应用重新启动。

windows可以用下面的命令终止进程：taskkill //PID 8580 //F 

我现在项目的前端运行在3000端口，后端运行在8080端口。所有的路由和数据库准确定义以doc目录下为准。请确保所有api都如文档预期的工作。

前端在登录时，获取用户在各个主题下的权限，然后读取这个权限内容，展示不同的功能。